"""
Testing for the scf.py module.
"""

import qm2
import pytest
import test_data

def test_buildgeom(mol, E_nuc):
#    assert qm2.scf.scfrun(mol) == E_nuc
    assert False

def test_diag(F, eps, C):
#    assert qm2.scf.diag(F) == eps, C
    assert False

def test_scfrun(mol, E_total):
#    assert qm2.scf.scfrun(mol) == E_total
    assert False


