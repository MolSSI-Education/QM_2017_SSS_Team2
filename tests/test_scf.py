"""
Testing for the scf.py module.
"""

import qm2
import pytest
import test_data

def test_build_geom(mol, E_nuc):
#    assert qm2.scf.build_geom(mol) == E_nuc
    assert False

def test_diag(F, eps, C):
#    assert qm2.scf.diag(F) == eps, C
    assert False

def test_scf_run(mol, E_total):
#    assert qm2.scf.scf_run(mol) == E_total
    assert False


